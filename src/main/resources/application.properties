spring.application.name=dream-project

# 서버 포트
server.port=8080

# MySQL 연결 설정
spring.datasource.url=jdbc:mysql://localhost:3306/dreamdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=gksrkdals0925!

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.open-in-view=false
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Dialect 설정 제거
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

# Thymeleaf 캐싱 끄기 (개발 편의성)
spring.thymeleaf.cache=false

logging.level.org.springframework.security=DEBUG